{
  "compilerOptions": {
    // keep existing settings...
    "baseUrl": ".",             // this is the base directory
    "paths": {
      "@/*": ["client/*"]       // tells TS that @/ means client/
    }
  },
  "include": ["client", "server"],
  "exclude": ["node_modules"]
}
